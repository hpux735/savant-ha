{
  "config": {
    "step": {
      "user": {
        "title": "Connect to Savant host",
        "description": "Enter the IP address (or hostname) of your Savant host. The control port is discovered automatically unless you provide one.",
        "data": {
          "host": "Host address",
          "port": "Port (optional)"
        }
      },
      "login": {
        "title": "Log in to Savant host",
        "description": "Log in with a host-local account ({host}, “{name}”). This is how the host authenticates a headless client.",
        "data": {
          "username": "Username",
          "password": "Password"
        }
      },
      "devices": {
        "title": "Savant devices found",
        "description": "{count} device(s) found on {host}. Choose which to import. Each device is placed in its configured room as an area — you can move them later in Home Assistant.",
        "data": {
          "devices": "Devices to import"
        }
      }
    },
    "error": {
      "discovery_failed": "Could not discover the host's control port. Provide an explicit port.",
      "invalid_auth": "Login failed — check the host-local username and password.",
      "no_auth_response": "The host did not respond to the login request. The host may only accept local login from the Savant app (see PROTOCOL.md §4.6).",
      "cannot_connect": "Could not connect to the host on the discovered port.",
      "no_devices": "Connected and authenticated, but no rooms or devices were reported by the host."
    },
    "abort": {
      "already_configured": "This host is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Savant advanced options",
        "description": "Optional. Update the local account or add a cached hostToken / cloud credentials. Leave blank to keep the current value.",
        "data": {
          "username": "Local username",
          "password": "Local password",
          "host_token": "Host token",
          "cloud_token": "Cloud token",
          "configuration_id": "Configuration ID",
          "rooms": "Extra room names (comma or newline separated)"
        }
      }
    }
  }
}